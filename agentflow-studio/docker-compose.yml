version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - FRONTEND_URL=http://localhost:3000
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
